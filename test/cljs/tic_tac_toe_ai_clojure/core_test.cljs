(ns tic-tac-toe-ai-clojure.core-test
  (:require [cljs.test :refer-macros [deftest testing is]]
            [tic-tac-toe-ai-clojure.core :as core]))

(deftest example-test
  (testing "example description"
    (is (= 1 1))))
